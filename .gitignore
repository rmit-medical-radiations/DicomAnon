# PyInstaller writes both of these on every build, locally and in CI
build
dist

.ipynb_checkpoints
.DS_Store
__pycache__/
tests/__pycache__/

# operational files from the hospital must never be committed to this public repo
*.xlsx
